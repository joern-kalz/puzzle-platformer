[package]
name = "puzzle-platformer"
version = "0.1.0"
edition = "2021"

[dependencies]
wasm-bindgen = "0.2"
image = { version = "0.25", default-features = false, features = ["png"] }
fastrand = "2.4.1"

[lib]
crate-type = ["cdylib"]
